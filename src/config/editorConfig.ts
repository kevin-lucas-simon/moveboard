/**
 * This file contains the configuration for the editor.
 * It is used to define the editor's behavior and properties.
 */
export const editorConfig: Record<string, any> = {
    hiddenJsonKeys: ["id", "type", "parent", "collapsed", "hidden"],
} // TODO raus damit!